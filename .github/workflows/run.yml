name: Run
on: [push]

jobs:
  macOS-latest:
    runs-on: macOS-latest
    strategy:
      matrix:
        node-version: [ 18.x ]

    steps:
      - name: Clone
        uses: actions/checkout@v1

      - name: Get CMake
        uses: lukka/get-cmake@latest

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Build
        run: |
          npm install
          npx gulp

      - name: link
        run: export DYLD_LIBRARY_PATH=./build/Release

      - name: Run
        run: node index.js
